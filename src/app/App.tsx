import { DefaultBoard, Header } from '@components'

import { AppWrapper, GlobalStyle } from './styled'

export const App = () => (
  <AppWrapper>
    {/* <Header /> */}
    <DefaultBoard />
    <GlobalStyle />
  </AppWrapper>
)
